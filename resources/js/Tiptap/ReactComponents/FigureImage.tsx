import { NodeViewProps, NodeViewWrapper } from "@tiptap/react"

const FigureImage: React.FC<NodeViewProps> = ({ node, updateAttributes }) => {

    return <NodeViewWrapper>
        <figure>

        </figure>
    </NodeViewWrapper>


}
